export default function App() {
  return (
    <main style={{ fontFamily: 'Inter, system-ui, sans-serif', padding: '4rem', color: '#122A3E', background: '#F6F2E9', minHeight: '100vh' }}>
      <h1>Keystone</h1>
      <p>UI shell arrives in Phase 3. The engine lives in <code>/src/engine</code> — run <code>npm test</code>.</p>
    </main>
  )
}
